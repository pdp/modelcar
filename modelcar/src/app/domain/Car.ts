import {Brand} from './Brand';
import {Color} from './Color';

export class Car {
  itemRef: string;
  brand: Brand;
  color: Color;
  limitedEdition: number;
  coupe: boolean;
  boxed: boolean;
  scale: string;
}
